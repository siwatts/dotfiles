# About

Kitty terminal, 04/01/2026

# Config

```bash
vim -d kitty.conf ~/.config/kitty/kitty.conf
ln -sf kitty.conf ~/.config/kitty/
ln -s clear-cursor.conf ~/.config/kitty/
```

Colour themes with `kitten themes`

